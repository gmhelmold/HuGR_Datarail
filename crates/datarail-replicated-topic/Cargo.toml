[package]
name = "datarail-replicated-topic"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "datarail-replicated-topic — a topic whose records are ALSO erasure-coded into a BlobStore, so a record survives losing any m shards (node-loss durability at 1.5x, composing topic + replication)."
[lints]
workspace = true
[dependencies]
datarail-topic = { path = "../datarail-topic" }
datarail-replication = { path = "../datarail-replication" }
datarail-blobstore = { path = "../datarail-blobstore" }
