[package]
name = "datarail-replication"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "datarail-replication — erasure-coded replicated durable store: shard a record across a BlobStore, reconstruct on loss (1.5x not 3x)."
[lints]
workspace = true
[dependencies]
datarail-erasure = { path = "../datarail-erasure" }
datarail-blobstore = { path = "../datarail-blobstore" }
