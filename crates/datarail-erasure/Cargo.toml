[package]
name = "datarail-erasure"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "datarail-erasure — minimal systematic Reed-Solomon (GF(2^8), Cauchy/MDS) erasure codec: the durability that costs 1.5× storage instead of Kafka's 3× replication, for the same fault tolerance."

[lints]
workspace = true

[dependencies]
