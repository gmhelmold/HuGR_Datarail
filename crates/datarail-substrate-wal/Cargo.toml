[package]
name = "datarail-substrate-wal"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "datarail write-ahead-log substrate — fsync-durable, O(1)-RAM, group-commit (SPEC 10 / DURABLE-LOG.md)."

[lints]
workspace = true

[dependencies]
datarail-core = { path = "../datarail-core" }
datarail-cofre = { path = "../datarail-cofre" }

[dev-dependencies]
datarail-rail = { path = "../datarail-rail" }
