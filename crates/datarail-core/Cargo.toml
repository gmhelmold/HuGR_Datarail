[package]
name = "datarail-core"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "datarail-core — the Cofre envelope + the rail/terminal contracts (transcribes the frozen SPEC)."

[lints]
workspace = true

[dependencies]
