[package]
name = "datarail-manifest"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "datarail-manifest — Merkle log, signed tree head, inclusion-proof delivery receipts (SPEC 04)."

[lints]
workspace = true

[dependencies]
datarail-crypto = { path = "../datarail-crypto" }
