[package]
name = "datarail-spec"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "datarail-spec — parse a declarative rail.toml into typed route/contract/key config (SPEC 09)."

[lints]
workspace = true

[dependencies]
datarail-core = { path = "../datarail-core" }
datarail-crypto = { path = "../datarail-crypto" }
datarail-terminal = { path = "../datarail-terminal" }
datarail-keymgmt = { path = "../datarail-keymgmt", features = ["age", "ssh-agent"] }
hex = "0.4"
