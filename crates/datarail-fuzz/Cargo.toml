[package]
name = "datarail-fuzz"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "datarail-fuzz — deterministic property/fuzz harness: hammers every untrusted-bytes parser (broker/netblob codecs, erasure, blobstore keys, replaylog frames) with random + adversarial input, proving no-panic + roundtrip identity. Zero-dep PRNG."
[lints]
workspace = true
[dependencies]
[dev-dependencies]
datarail-broker = { path = "../datarail-broker" }
datarail-erasure = { path = "../datarail-erasure" }
datarail-blobstore = { path = "../datarail-blobstore" }
datarail-netblob = { path = "../datarail-netblob" }
datarail-replaylog = { path = "../datarail-replaylog" }
datarail-kafka = { path = "../datarail-kafka" }
datarail-cofre = { path = "../datarail-cofre" }
