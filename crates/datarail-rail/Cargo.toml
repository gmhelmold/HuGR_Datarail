[package]
name = "datarail-rail"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "datarail-rail — ephemeral, substrate-polymorphic transport moving cofres by header+seal (SPEC 07)."

[lints]
workspace = true

[dependencies]
datarail-core = { path = "../datarail-core" }
datarail-cofre = { path = "../datarail-cofre" }
# E5 DoS proof-of-IP cookie uses hmac_blake3; AC-1 tests also use verifying_key/verify.
datarail-crypto = { path = "../datarail-crypto" }
